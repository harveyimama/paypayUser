spring:
  cloud:
    stream:
      kafka:
        binder:
          brokers: localhost:9092
      bindings:
        user-in:
          destination: user
          contentType: application/json
        user-out:
          destination: user
          contentType: application/json
          
   
